meta {
  name: Adicionar
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/v1/estados
  body: json
  auth: inherit
}

body:json {
  {
    "nome": "Sergipe"
  }
}

assert {
  res.status: eq 201
}

docs {
  `@CheckSecurity.Estados.PodeEditar` — SCOPE_WRITE e EDITAR_ESTADOS
}
