meta {
  name: Remover
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/v1/formasPagamento/{{formaPagamentoId}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

docs {
  `@CheckSecurity.FormasPagamento.PodeEditar`
}
