meta {
  name: Cidades V2
  seq: 20
}

docs {
  Versionamento por URI. O contrato muda: `nome` → `nomeCidade` e o objeto
  aninhado `estado.id` vira o escalar `idEstado`.

  A v2 não tem `@CheckSecurity` — basta um token válido.
}
